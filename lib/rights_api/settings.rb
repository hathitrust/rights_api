# frozen_string_literal: true

# require "ettin"
require "sinatra"
module RightsAPI
end

# RightsAPI::Settings = Ettin.for(Ettin.settings_files("config", settings.environment))
# RightsAPI::Settings.environment = settings.environment
